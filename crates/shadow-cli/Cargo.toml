[package]
name = "shadow-cli"
version = "0.27.0"
edition = "2024"
description = "CLI interface for Shadow file encryption"

[dependencies]
shadow-domain = { path = "../shadow-domain" }
shadow-application = { path = "../shadow-application" }
thiserror = "2.0"
clap = { version = "4", features = ["derive"] }
glob = "0.3"